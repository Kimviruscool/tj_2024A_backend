[메모리 설계]
- 최대 제품 개수는 3개
- 제품의 속성은 제품코드 , 제품명 , 제품가격 3개
//설계 조건
  1. 배열 사용금지 2. 클래스/객체 사용금지

[기능 구현]
1. 등록시
    //제품코드,제품명,제품가격을 각 입력받아 저장
    //저장 가능한 제품 수가 초과할경우 저장 불가능 안내
2. 출력시
    //현재 저장된 제품들의 속성 모두 출력
    //없는 제품은 출력하지 마세요.
3. 수정시
    //제품코드를 입력받아 일치한 제품코드가 존재하면 수정 아니면 수정불가
    //일치했을때 제품명과 제품가격을 새롭게 입력받아 수정처리
4. 삭제시
    //제품코드를 입력받아 일치한 제품코드가 존재하면 제품 삭제 아니면 삭제 불가

[샘플]
 Scanner scanner = new Scanner(System.in);

while (true){
System.out.println("1.등록(C) 2.출력(R) 3.수정(U) 4.삭제(D) 선택 : "); int ch = scanner.nextInt();
if (ch == 1){}
else if (ch == 2){}
else if (ch == 3){}
else if (ch == 4){}
else if (ch == 5){System.out.println("[안내] 프로그램을 종료합니다."); break;}
else {System.out.println("다시 선택하시오");}
}

1. 제품CRUD 변수 버전

2. 제품CRUD 배열 버전

4. 제품CRUD 객체와 배열 버전

5. 제품CURD 함수활용 객체와 배열 버전