접근제한자

public : (동일클래스,동일패키지,다른패키지)공개 > 모든곳 접근 가능

protected : (동일클래스,동일패키지,*다른패키지)일부공개 > 같은 패키지(폴더) 내에서 접근가능
            단 자식클래스가 다른 패키지에 존재하면 자식클래스는 가능

default(생략시) : (동일클래스,동일패키지)기본값 (공백) > 같은 패키지(폴더) 내에서 접근 가능

private : (동일클래스)비공개 > 현재 클래스내에서만 접근가능

변수명
함수명
생성자
클래스
인터페이스
